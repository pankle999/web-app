<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Dashboard</title>
<link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="dashboard-container">
    <h2>Dashboard Data (A:I)</h2>

    <% if (!dashboard || dashboard.length === 0) { %>
      <p>No dashboard data available.</p>
    <% } else { %>
      <ul>
        <% dashboard.forEach(d => { %>
          <li><strong><%= d.a %></strong>: <%= d.i %></li>
        <% }) %>
      </ul>
    <% } %>

    <% if (error) { %>
      <p style="color:red;">Failed to fetch latest data: <%= error %></p>
    <% } %>

    <a href="/logout">Logout</a>
  </div>
</body>
</html>
