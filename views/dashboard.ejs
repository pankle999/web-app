<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Dashboard</title>
<link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="dashboard-container">
    <h2>Weekly Updates (Processed)</h2>
    <% if (!updates || updates.length === 0) { %>
      <p>No weekly updates available.</p>
    <% } else { %>
      <ul>
        <% updates.forEach(u => { %>
          <li>Row <%= u.row %>: <%= u.newBank %></li>
        <% }) %>
      </ul>
    <% } %>

    <h2>Dashboard Data (A:I)</h2>
    <% if (!dashboard || dashboard.length === 0) { %>
      <p>No dashboard data available.</p>
    <% } else { %>
      <ul>
        <% dashboard.forEach(d => { %>
          <li><strong><%= d.a %></strong>: <%= d.i %></li>
        <% }) %>
      </ul>
    <% } %>

    <% if (error) { %>
      <p style="color:red;"><%= error %></p>
    <% } %>

    <a href="/logout">Logout</a>
  </div>
</body>
</html>
